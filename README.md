# rezeptbuch
A Java recipe book I once made for a two-day university project

Fairly self-explainatory, run ./gradlew to compile/start the program.
The project already contains a custom gradle configuration.

Persistence is implemented via the RezeptbuchSave file.
